theme: jekyll-theme-minimal
title: VIKAS SINGH
description: Data Science Lead
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      author: "Your Name"

# Add markdown support
markdown: kramdown

# Plugins
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Dark mode CSS
sass:
  style: compressed
